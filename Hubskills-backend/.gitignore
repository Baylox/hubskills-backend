# Maven
/target
/target/
!.mvn/wrapper/maven-wrapper.jar
!**/src/main/resources/*.properties
!**/src/test/resources/*.properties

# IDEs
.idea/
.vscode/
*.iml
*.ipr
*.iws
.classpath
.project
.settings/
.springBeans

# Local Config
.env
*.local.properties
# Ignore all properties files in resources? No, usually we want application.properties, but not secrets.
# We already cleaned application.properties so it is safe to commit.

# OS Generated
.DS_Store
Thumbs.db

